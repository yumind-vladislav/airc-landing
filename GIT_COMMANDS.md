# Git-команды для деплоя AI4C

Шпаргалка по Git для быстрого деплоя лендинга на Timeweb.

---

## Первый деплой (один раз)

### 1. Инициализация Git в проекте

```bash
# Перейди в папку проекта
cd /Users/astro_vlad420/Desktop/AI4C

# Инициализируй Git (если еще не сделано)
git init

# Проверь статус
git status
```

---

### 2. Добавь файлы в Git

```bash
# Добавь все файлы из папки site/
git add site/

# Или добавь всё сразу (включая гайды)
git add .

# Проверь, что добавлено
git status
```

---

### 3. Создай первый коммит

```bash
# Создай коммит с описанием
git commit -m "Initial commit: AI4C landing"

# Проверь историю
git log --oneline
```

---

### 4. Создай репозиторий на GitHub

1. Зайди на **[github.com](https://github.com)**
2. Нажми **"New repository"**
3. Название: `ai4c-landing`
4. Приватность: **Public** (или Private)
5. **НЕ создавай** README, .gitignore, LICENSE (уже есть локально)
6. Нажми **"Create repository"**

---

### 5. Подключи репозиторий GitHub

```bash
# Подключи удаленный репозиторий
git remote add origin https://github.com/ТвойUsername/ai4c-landing.git

# Проверь, что подключено
git remote -v

# Отправь код на GitHub
git push -u origin main
```

**Если просит логин/пароль:**
- GitHub больше не поддерживает пароли
- Создай **Personal Access Token**:
  1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
  2. Generate new token → Выбери права: `repo` (полный доступ к репозиториям)
  3. Скопируй токен (показывается один раз!)
  4. Используй токен вместо пароля

---

### 6. Подключи Git-деплой в Timeweb

1. Панель Timeweb → **"Git"** → **"Добавить репозиторий"**
2. Заполни:
   - **URL:** `https://github.com/ТвойUsername/ai4c-landing.git`
   - **Ветка:** `main`
   - **Путь на сервере:** `public_html/`
   - **Поддиректория в репозитории:** `site/`
3. Включи **"Автодеплой при push"** ✅
4. Нажми **"Добавить"**

---

## Обновление лендинга (каждый раз)

### Workflow: Внес изменения → Push → Сайт обновился

```bash
# 1. Проверь, что изменилось
git status

# 2. Добавь изменения
git add site/

# Или добавь конкретный файл
git add site/index.html

# 3. Создай коммит с описанием
git commit -m "Обновил текст hero секции"

# 4. Отправь на GitHub
git push

# 5. Готово! Через 10-30 секунд изменения на сайте ✅
```

---

## Полезные команды

### Проверить статус

```bash
# Показать измененные файлы
git status

# Показать изменения в файлах (до add)
git diff

# Показать изменения после add (перед commit)
git diff --staged
```

---

### История коммитов

```bash
# Показать историю коммитов (кратко)
git log --oneline

# Показать историю с изменениями
git log -p

# Показать последние 5 коммитов
git log -5
```

---

### Отменить изменения

```bash
# Отменить изменения в файле (до add)
git checkout -- site/index.html

# Убрать файл из staging (после add, до commit)
git reset HEAD site/index.html

# Отменить последний коммит (но оставить изменения)
git reset --soft HEAD~1

# Отменить последний коммит (и удалить изменения) ⚠️ Опасно!
git reset --hard HEAD~1
```

---

### Ветки (для продвинутых)

```bash
# Создать новую ветку (для тестирования)
git checkout -b test-feature

# Переключиться на главную ветку
git checkout main

# Влить изменения из ветки в main
git merge test-feature

# Удалить ветку
git branch -d test-feature
```

---

## Частые проблемы

### Проблема: `error: failed to push some refs`

**Причина:** На GitHub есть изменения, которых нет локально.

**Решение:**
```bash
# Скачай изменения с GitHub
git pull origin main

# Если есть конфликты — разреши их вручную
# Затем снова push
git push
```

---

### Проблема: `fatal: remote origin already exists`

**Причина:** Репозиторий уже подключен.

**Решение:**
```bash
# Удали старый remote
git remote remove origin

# Добавь новый
git remote add origin https://github.com/ТвойUsername/ai4c-landing.git
```

---

### Проблема: Забыл добавить файлы в коммит

**Решение:**
```bash
# Добавь забытые файлы
git add site/забытый-файл.js

# Измени последний коммит (добавь файлы)
git commit --amend --no-edit

# Или измени последний коммит с новым сообщением
git commit --amend -m "Обновленное сообщение коммита"

# Отправь с флагом --force (только для последнего коммита!)
git push --force
```

---

### Проблема: Хочу удалить файл из Git (но оставить локально)

**Решение:**
```bash
# Удали файл из Git (но оставь на диске)
git rm --cached site/.DS_Store

# Добавь в .gitignore, чтобы не добавлялся снова
echo ".DS_Store" >> site/.gitignore

# Закоммить
git commit -m "Удалил .DS_Store из Git"
git push
```

---

## Шпаргалка: Типичный workflow

```bash
# Утром (начало работы)
cd /Users/astro_vlad420/Desktop/AI4C
git status                           # Проверь статус
git pull                             # Скачай изменения (если работаешь с командой)

# Работа...
# Внес изменения в site/index.html

# Вечером (конец работы)
git status                           # Проверь, что изменилось
git diff                             # Посмотри изменения
git add site/                        # Добавь изменения
git commit -m "Обновил hero секцию"  # Создай коммит
git push                             # Отправь на GitHub → сайт обновится

# Готово! ✅
```

---

## Best Practices

### Хорошие сообщения коммитов

```bash
# ✅ Хорошо (коротко и ясно)
git commit -m "Добавил кейс студента #4"
git commit -m "Исправил ошибку в форме"
git commit -m "Обновил цвета кнопок"

# ❌ Плохо (неинформативно)
git commit -m "update"
git commit -m "fix"
git commit -m "changes"
```

---

### Частота коммитов

- **Делай коммиты часто** (после каждой логической части работы)
- **Не копи изменения** (не делай один коммит с 50 файлами)
- **Push регулярно** (чтобы был бэкап на GitHub)

---

### .gitignore

Файл `.gitignore` уже создан в `site/.gitignore`.

**Что в нем:**
- `.DS_Store` (macOS)
- `.vscode/`, `.idea/` (редакторы)
- `*.log` (логи)
- `.env` (переменные окружения)

**Если нужно добавить:**
```bash
echo "новый-файл.txt" >> site/.gitignore
git add site/.gitignore
git commit -m "Обновил .gitignore"
git push
```

---

## Резервное копирование

### Скачать резервную копию с GitHub

```bash
# Клонировать репозиторий в новую папку
git clone https://github.com/ТвойUsername/ai4c-landing.git AI4C-backup

# Или скачать как ZIP
# GitHub → Code → Download ZIP
```

---

## Итого

**Команды на каждый день:**
```bash
git status        # Проверить статус
git add site/     # Добавить изменения
git commit -m ""  # Создать коммит
git push          # Отправить на GitHub
```

**Всё остальное — по мере необходимости.**

---

**Удачи с Git! 🚀**
