# Деплой AI4C на Timeweb — Простая инструкция

Пошаговый гайд для размещения лендинга **AI FOR CREATORS 4.0** на хостинге Timeweb.

---

## Что нужно перед началом

- [ ] Банковская карта (для оплаты хостинга)
- [ ] Email (для регистрации)
- [ ] Папка `site/` с лендингом (уже готова ✅)

**Время деплоя:** 30 минут

---

## Способ 1: Быстрый деплой (через Файловый менеджер)

Самый простой способ. Подходит, если не планируешь частые обновления.

### Шаг 1: Регистрация и покупка хостинга

1. Зайди на **[timeweb.com](https://timeweb.com)**
2. Нажми **"Хостинг"** → **"Виртуальный хостинг"**
3. Выбери тариф **"Старт"** (199₽/мес)
   - 10 ГБ NVMe SSD
   - Безлимит сайтов
   - Бесплатный SSL

4. Нажми **"Заказать"**
5. Зарегистрируйся (Email + придумай пароль)
6. Оплати (карта, PayPal, ЮMoney)

**Бонус:** Первые 14 дней — бесплатно (можно протестировать).

---

### Шаг 2: Покупка домена

1. В панели Timeweb → **"Домены"** → **"Зарегистрировать домен"**
2. Введи: `aiforcreators.ru`
3. Проверь доступность
4. Оплати (~200-500₽/год)

**Если домен занят:**
- Попробуй: `ai4c.ru`, `aiforcreators.online`, `ai-creators.ru`

**Альтернатива:**
- Купи домен на **reg.ru** или **nic.ru**, потом привяжи к Timeweb (инструкция в панели)

---

### Шаг 3: Привязка домена к хостингу

1. Панель Timeweb → **"Сайты"** → **"Добавить сайт"**
2. Введи домен: `aiforcreators.ru`
3. Нажми **"Добавить"**
4. Подожди 5-10 минут (пока DNS обновится)

---

### Шаг 4: Загрузка файлов лендинга

#### Вариант A: Через Файловый менеджер (проще)

1. Панель Timeweb → **"Файловый менеджер"**
2. Открой папку `public_html/` (корень сайта)
3. **Удали** все файлы внутри (если есть)
4. Нажми **"Загрузить"** → выбери файлы из папки `site/`:
   - `index.html`
   - Папка `styles/`
   - Папка `scripts/`
   - Папка `assets/`

5. Загрузка займет 1-2 минуты

**Структура должна быть:**
```
public_html/
├── index.html          ← главный файл
├── styles/
│   └── custom.css
├── scripts/
│   └── main.js
└── assets/
    └── images/
        ├── hero-main.jpg
        ├── фото Арины.jpg
        └── ...
```

#### Вариант B: Через FTP (для опытных)

1. Скачай **[FileZilla](https://filezilla-project.org/download.php?type=client)**
2. Панель Timeweb → **"FTP/SSH"** → посмотри данные:
   - Хост: `ftp.timeweb.ru`
   - Логин: `u123456` (твой ID)
   - Пароль: (из панели)

3. Открой FileZilla → подключись
4. Слева: локальная папка `site/`
5. Справа: удаленная папка `public_html/`
6. Перетащи всё из `site/` в `public_html/`

---

### Шаг 5: Проверка

1. Открой браузер
2. Введи: `http://aiforcreators.ru` (или твой домен)
3. Лендинг должен открыться ✅

**Если не открывается:**
- Подожди 10-15 минут (DNS обновляется)
- Проверь, что файлы в `public_html/` (не в подпапке!)
- Проверь, что файл называется `index.html` (не `Index.html` или `home.html`)

---

### Шаг 6: Включение SSL (HTTPS)

1. Панель Timeweb → **"Сайты"** → выбери `aiforcreators.ru`
2. Вкладка **"SSL"**
3. Нажми **"Установить Let's Encrypt SSL"** (бесплатно)
4. Подожди 5 минут → SSL установлен ✅

Теперь сайт доступен по `https://aiforcreators.ru`

---

### Шаг 7: Настройка формы (обязательно!)

Форма на лендинге пока не работает (отправка только в консоль браузера).

**Варианты:**

#### A) AmoCRM (рекомендую)
1. Зарегистрируйся на **[amocrm.ru](https://amocrm.ru)** (бесплатный тариф)
2. Создай воронку "Предзапись"
3. Скопируй webhook URL из AmoCRM
4. Открой файл `site/scripts/main.js` → строка 82
5. Вставь код отправки в AmoCRM (инструкция в `FORM_INTEGRATION.md`)

#### B) Email (простой вариант)
1. Используй сервис **[Formspree](https://formspree.io)** (бесплатно до 50 форм/мес)
2. Зарегистрируйся → создай новую форму
3. Скопируй endpoint URL
4. Открой `site/scripts/main.js` → строка 82
5. Вставь:
```javascript
fetch('https://formspree.io/f/YOUR_FORM_ID', {
    method: 'POST',
    body: JSON.stringify(data),
    headers: { 'Content-Type': 'application/json' }
});
```

#### C) Telegram-бот (для быстрого старта)
1. Создай бота через **[@BotFather](https://t.me/botfather)**
2. Получи токен
3. Используй API Telegram для отправки сообщений
4. Инструкция в `FORM_INTEGRATION.md`

**Важно:** Без настройки формы заявки будут теряться!

---

### ✅ Готово!

Лендинг опубликован на `https://aiforcreators.ru`

---

## Способ 2: Git-деплой (для частых обновлений)

Если планируешь часто обновлять лендинг — используй Git. После первой настройки обновления = один `git push`.

### Шаг 1-3: Как в Способе 1

Купи хостинг, домен, привяжи домен (шаги 1-3 выше).

---

### Шаг 4: Создай репозиторий на GitHub

1. Зайди на **[github.com](https://github.com)**
2. Нажми **"New repository"**
3. Название: `ai4c-landing`
4. Приватность: **Public** (или Private, если хочешь)
5. Нажми **"Create repository"**

---

### Шаг 5: Загрузи код в GitHub

Открой терминал (или Git Bash на Windows):

```bash
# Перейди в папку проекта
cd /Users/astro_vlad420/Desktop/AI4C

# Инициализируй Git (если еще не сделано)
git init

# Добавь все файлы из папки site/
git add site/

# Создай коммит
git commit -m "Initial commit: AI4C landing"

# Подключи репозиторий GitHub
git remote add origin https://github.com/ТвойUsername/ai4c-landing.git

# Отправь код на GitHub
git push -u origin main
```

**Если просит логин/пароль:**
- Используй Personal Access Token (Settings → Developer settings → Personal access tokens)

---

### Шаг 6: Подключи Git-деплой в Timeweb

1. Панель Timeweb → **"Git"** → **"Добавить репозиторий"**
2. Заполни:
   - **URL репозитория:** `https://github.com/ТвойUsername/ai4c-landing.git`
   - **Ветка:** `main`
   - **Путь на сервере:** `public_html/`
   - **Поддиректория в репозитории:** `site/` (важно!)

3. Нажми **"Добавить"**
4. Включи **"Автодеплой при push"** ✅

---

### Шаг 7: Проверка

1. Panель Git → нажми **"Обновить"** (первый деплой)
2. Открой `https://aiforcreators.ru` → лендинг работает ✅

**Теперь при каждом `git push`:**
- Код автоматически попадает на сервер
- Не нужно загружать файлы вручную 🔥

---

### Как обновлять лендинг (после настройки Git)

1. Внеси изменения в файлы (например, `site/index.html`)
2. В терминале:
```bash
git add site/
git commit -m "Обновил текст hero секции"
git push
```
3. Готово! Через 10-30 секунд изменения на сайте ✅

---

## Частые проблемы и решения

### Проблема: Сайт не открывается

**Решение:**
1. Проверь, что домен привязан (Панель → Сайты → должен быть твой домен)
2. Подожди 10-15 минут (DNS обновляется)
3. Проверь, что файлы в `public_html/` (не в подпапке!)
4. Проверь, что главный файл называется `index.html`

---

### Проблема: CSS/JS не загружаются

**Решение:**
1. Проверь структуру папок:
```
public_html/
├── index.html
├── styles/custom.css    ← правильный путь
└── scripts/main.js
```

2. Открой `index.html` → проверь пути:
```html
<link rel="stylesheet" href="styles/custom.css">  ✅
<script src="scripts/main.js"></script>           ✅

<!-- НЕ должно быть: -->
<link rel="stylesheet" href="/styles/custom.css"> ❌
<link rel="stylesheet" href="../styles/custom.css"> ❌
```

---

### Проблема: Изображения не загружаются

**Решение:**
1. Проверь, что папка `assets/` загружена полностью
2. Проверь пути в `index.html`:
```html
<img src="assets/images/hero-main.jpg">  ✅
```

3. Проверь, что имена файлов совпадают (с учетом регистра):
   - `hero-main.jpg` ≠ `Hero-Main.jpg` (на Linux-серверах)

---

### Проблема: Форма не работает

**Решение:**
1. Форма пока не настроена (отправка только в консоль браузера)
2. Настрой интеграцию (см. Шаг 7 или файл `FORM_INTEGRATION.md`)
3. Варианты: AmoCRM, Formspree, Telegram-бот

---

### Проблема: Git-деплой не работает

**Решение:**
1. Проверь, что поддиректория указана правильно: `site/`
2. Проверь, что ветка правильная: `main` (или `master`)
3. Проверь логи деплоя (Панель → Git → Логи)
4. Если ошибка SSH key: добавь публичный ключ в GitHub (Settings → SSH keys)

---

## Безопасность и оптимизация

### Добавь robots.txt

Создай файл `site/robots.txt`:
```
User-agent: *
Allow: /

Sitemap: https://aiforcreators.ru/sitemap.xml
```

Загрузи в `public_html/`

---

### Добавь favicon

1. Создай иконку (32x32px, 64x64px)
2. Сохрани как `favicon.ico`
3. Загрузи в `public_html/`
4. Или используй генератор: [favicon.io](https://favicon.io)

---

### Настрой Яндекс.Метрику

1. Создай счетчик на **[metrika.yandex.ru](https://metrika.yandex.ru)**
2. Скопируй код счетчика
3. Вставь в `site/index.html` перед `</head>`

---

## Чеклист деплоя

- [ ] Куплен хостинг Timeweb (тариф Старт)
- [ ] Куплен домен (aiforcreators.ru)
- [ ] Домен привязан к хостингу
- [ ] Файлы загружены в `public_html/`
- [ ] Сайт открывается в браузере
- [ ] SSL установлен (HTTPS работает)
- [ ] Форма настроена (AmoCRM / Formspree / Telegram)
- [ ] Яндекс.Метрика подключена (опционально)
- [ ] robots.txt добавлен (опционально)
- [ ] favicon добавлен (опционально)

---

## Итого

**Способ 1 (Файловый менеджер):**
- Время: 30 минут
- Сложность: Легко
- Подходит: Для разового деплоя

**Способ 2 (Git-деплой):**
- Время: 40 минут (первая настройка)
- Сложность: Средне
- Подходит: Для частых обновлений

---

## Следующие шаги

1. [ ] Деплой лендинга (следуй инструкции выше)
2. [ ] Настройка формы (см. `FORM_INTEGRATION.md`)
3. [ ] Добавление политики конфиденциальности (обязательно для 152-ФЗ)
4. [ ] Подключение аналитики (Яндекс.Метрика)

---

## Помощь

**Техподдержка Timeweb:**
- Тикеты: [timeweb.com/ru/support](https://timeweb.com/ru/support)
- Онлайн-чат: в панели управления (правый нижний угол)
- Telegram: [@timeweb_bot](https://t.me/timeweb_bot)

**Документация:**
- [help.timeweb.ru](https://help.timeweb.ru)

---

**Удачи с деплоем! 🚀**
