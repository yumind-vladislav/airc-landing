# Интеграция формы предзаписи AI4C

Инструкции по подключению формы к различным сервисам для приема заявок.

---

## Текущее состояние

Форма на лендинге (`site/index.html`) собирает данные:
- Имя
- Телефон
- Telegram

Данные **НЕ отправляются** на сервер (только в консоль браузера для теста).

**Нужно:** Подключить отправку на email, CRM или Telegram.

---

## Вариант 1: AmoCRM (рекомендуется для бизнеса)

Лучший вариант для управления заявками и воронкой продаж.

### Преимущества
- ✅ Российский сервис (соответствие 152-ФЗ)
- ✅ Бесплатный тариф (до 2 пользователей)
- ✅ Воронка продаж, задачи, аналитика
- ✅ Интеграции (Email, Telegram, WhatsApp)

### Шаги

#### 1. Регистрация в AmoCRM

1. Зайди на **[amocrm.ru](https://amocrm.ru)**
2. Нажми **"Попробовать бесплатно"**
3. Зарегистрируйся (Email, пароль)
4. Создай аккаунт (название: "AI4C")

#### 2. Создай воронку

1. Настройки → **Воронки и статусы**
2. Создай воронку: **"Предзапись на курс"**
3. Статусы:
   - Новая заявка
   - Связались
   - Подтверждена предзапись
   - Оплачено
   - Отказ

#### 3. Получи API-ключ

1. Настройки → **Интеграции** → **API**
2. Скопируй:
   - **Поддомен:** `yoursubdomain.amocrm.ru`
   - **API-ключ:** (длинная строка)

#### 4. Интеграция с лендингом

Открой файл `site/scripts/main.js` → найди функцию `initPresaleForm()` (строка 56).

Замени строки 80-82:

**Было:**
```javascript
// Здесь можно добавить отправку на сервер
// Например: fetch('/api/presale', { method: 'POST', body: JSON.stringify(data) })
```

**Стало:**
```javascript
// Отправка в AmoCRM
fetch('https://yoursubdomain.amocrm.ru/api/v4/leads', {
    method: 'POST',
    headers: {
        'Authorization': 'Bearer YOUR_API_KEY',
        'Content-Type': 'application/json'
    },
    body: JSON.stringify({
        name: `Заявка: ${data.name}`,
        custom_fields_values: [
            { field_id: 123456, values: [{ value: data.phone }] },
            { field_id: 123457, values: [{ value: data.telegram }] }
        ]
    })
})
.then(response => response.json())
.then(result => {
    console.log('Заявка отправлена в AmoCRM:', result);
})
.catch(error => {
    console.error('Ошибка отправки:', error);
});
```

**Замени:**
- `yoursubdomain` → твой поддомен AmoCRM
- `YOUR_API_KEY` → твой API-ключ
- `field_id` → ID полей "Телефон" и "Telegram" (найди в AmoCRM → Настройки → Поля)

#### 5. Проверка

1. Открой лендинг в браузере
2. Заполни форму
3. Отправь
4. Проверь AmoCRM → должна появиться новая сделка ✅

---

## Вариант 2: Formspree (проще всего)

Самый простой способ — форма отправляет на email.

### Преимущества
- ✅ Настройка за 5 минут
- ✅ Бесплатно (до 50 форм/мес)
- ✅ Email-уведомления
- ✅ Спам-защита

### Шаги

#### 1. Регистрация

1. Зайди на **[formspree.io](https://formspree.io)**
2. Нажми **"Get Started"**
3. Зарегистрируйся (Email, пароль)

#### 2. Создай форму

1. Нажми **"New Form"**
2. Название: `AI4C Presale`
3. Email для уведомлений: `hello@aiforcreators.ru`
4. Скопируй **Form Endpoint**: `https://formspree.io/f/xyzabc123`

#### 3. Интеграция с лендингом

Открой `site/scripts/main.js` → замени строки 80-82:

```javascript
// Отправка через Formspree
fetch('https://formspree.io/f/xyzabc123', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
        name: data.name,
        phone: data.phone,
        telegram: data.telegram,
        _subject: `Новая заявка от ${data.name}`
    })
})
.then(response => {
    if (response.ok) {
        console.log('Заявка отправлена на email');
    }
})
.catch(error => {
    console.error('Ошибка отправки:', error);
});
```

**Замени:** `xyzabc123` → твой Form ID

#### 4. Проверка

1. Заполни форму на лендинге
2. Отправь
3. Проверь email → должно прийти уведомление ✅

---

## Вариант 3: Telegram-бот (для быстрого старта)

Заявки приходят напрямую в Telegram.

### Преимущества
- ✅ Бесплатно навсегда
- ✅ Мгновенные уведомления
- ✅ Не нужна регистрация в сервисах

### Шаги

#### 1. Создай Telegram-бота

1. Открой Telegram → найди **[@BotFather](https://t.me/botfather)**
2. Отправь команду: `/newbot`
3. Введи имя: `AI4C Presale Bot`
4. Введи username: `ai4c_presale_bot`
5. Скопируй **токен**: `123456789:ABCDEFGHIJKLMNOPQRSTUVWXYZ`

#### 2. Получи Chat ID

1. Открой бота в Telegram
2. Отправь любое сообщение
3. Открой в браузере:
```
https://api.telegram.org/bot123456789:ABCDEFGHIJKLMNOPQRSTUVWXYZ/getUpdates
```
4. Найди `"chat":{"id":987654321}` → это твой Chat ID

#### 3. Интеграция с лендингом

Открой `site/scripts/main.js` → замени строки 80-82:

```javascript
// Отправка в Telegram
const botToken = '123456789:ABCDEFGHIJKLMNOPQRSTUVWXYZ';
const chatId = '987654321';

const message = `
🆕 Новая заявка на AI4C!

👤 Имя: ${data.name}
📞 Телефон: ${data.phone}
📱 Telegram: @${data.telegram}
`;

fetch(`https://api.telegram.org/bot${botToken}/sendMessage`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
        chat_id: chatId,
        text: message,
        parse_mode: 'HTML'
    })
})
.then(response => response.json())
.then(result => {
    console.log('Заявка отправлена в Telegram:', result);
})
.catch(error => {
    console.error('Ошибка отправки:', error);
});
```

**Замени:**
- `123456789:ABCDEFGHIJKLMNOPQRSTUVWXYZ` → токен бота
- `987654321` → твой Chat ID

#### 4. Проверка

1. Заполни форму на лендинге
2. Отправь
3. Проверь Telegram → должно прийти сообщение от бота ✅

---

## Вариант 4: Google Sheets (хранение в таблице)

Все заявки попадают в Google Таблицу.

### Преимущества
- ✅ Бесплатно
- ✅ Удобно для экспорта (CSV, Excel)
- ✅ Можно настроить email-уведомления (через скрипт)

### Шаги

#### 1. Создай Google Таблицу

1. Зайди на **[sheets.google.com](https://sheets.google.com)**
2. Создай таблицу: **"AI4C Заявки"**
3. Заголовки (первая строка):
   - A1: `Дата`
   - B1: `Имя`
   - C1: `Телефон`
   - D1: `Telegram`

#### 2. Создай Google Apps Script

1. В таблице: **Расширения** → **Apps Script**
2. Вставь код:

```javascript
function doPost(e) {
  const sheet = SpreadsheetApp.getActiveSpreadsheet().getActiveSheet();
  const data = JSON.parse(e.postData.contents);

  sheet.appendRow([
    new Date(),
    data.name,
    data.phone,
    data.telegram
  ]);

  return ContentService.createTextOutput(JSON.stringify({ success: true }))
    .setMimeType(ContentService.MimeType.JSON);
}
```

3. Нажми **Сохранить**
4. **Развернуть** → **Новое развертывание**
5. Тип: **Веб-приложение**
6. Доступ: **Все, включая анонимных**
7. Скопируй **URL**: `https://script.google.com/macros/s/.../exec`

#### 3. Интеграция с лендингом

Открой `site/scripts/main.js` → замени строки 80-82:

```javascript
// Отправка в Google Sheets
fetch('https://script.google.com/macros/s/.../exec', {
    method: 'POST',
    body: JSON.stringify(data)
})
.then(response => response.json())
.then(result => {
    console.log('Заявка добавлена в Google Sheets:', result);
})
.catch(error => {
    console.error('Ошибка отправки:', error);
});
```

**Замени:** `.../exec` → твой URL из Apps Script

#### 4. Проверка

1. Заполни форму
2. Отправь
3. Проверь Google Таблицу → должна появиться новая строка ✅

---

## Рекомендации

### Для бизнеса (профессиональный подход)
**Используй:** AmoCRM + Email-уведомления
- Полноценная CRM
- Аналитика и воронка
- Интеграции с другими сервисами

### Для быстрого старта (MVP)
**Используй:** Telegram-бот
- Настройка за 10 минут
- Бесплатно
- Мгновенные уведомления

### Для хранения данных
**Используй:** Google Sheets
- Все заявки в одном месте
- Удобно экспортировать
- Можно настроить автоматизацию

---

## Важно: Соблюдение 152-ФЗ

Если используешь AmoCRM, Битрикс24 или другие российские сервисы — данные хранятся в РФ (соответствие закону).

Если используешь Formspree, Google Sheets — данные за рубежом (нужно уведомление Роскомнадзора).

**Безопасный вариант для РФ:**
- AmoCRM (российский)
- Битрикс24 (российский)
- Telegram (условно безопасен)

---

## Тестирование формы

После интеграции:
1. Открой лендинг в режиме инкогнито
2. Заполни форму тестовыми данными
3. Отправь
4. Проверь, что заявка пришла (AmoCRM / Email / Telegram / Google Sheets)
5. Проверь Success-сообщение на лендинге

---

## Что дальше

После настройки формы:
1. [ ] Добавь Политику конфиденциальности (обязательно для 152-ФЗ)
2. [ ] Настрой автоответ (Email / Telegram)
3. [ ] Подключи аналитику (Яндекс.Метрика → цели)
4. [ ] Настрой уведомления для себя (Push / Email)

---

**Форма — сердце лендинга. Настрой её правильно!**
